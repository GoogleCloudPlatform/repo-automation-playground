/* eslint-disable */

const assert = require('assert');

// Both `describe` lines should be un-indented
describe('region_tag test', () => {
describe('region_tag test', () => {
	it('return_one', () => {
		assert.equal(1, 1);
	});

	it('return_two', () => {
		assert.equal(2, 2);
	});
});
});