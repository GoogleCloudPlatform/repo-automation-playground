const assert = require('assert');

describe('region_tag test', () => {
	it('return_one', () => {
		assert.equal(1, 1);
	});
});

describe('region_tag test', () => {
	it('return_two', () => {
		assert.equal(2, 2);
	});
});